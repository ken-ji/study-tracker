# タスクリスト（MVP）

実装順序は依存関係に従う。下層モジュールから上層へ順番に実装する。

---

## フェーズ 0：プロジェクト初期化

- [x] **T-001** ディレクトリ構成を作成する（空ファイル含む）
- [x] **T-002** `index.html` のシェルを作成する（ナビ・セクション骨格）
- [x] **T-003** `manifest.json` を作成する
- [x] **T-004** PWA アイコン（192x192・512x512）を用意する
- [x] **T-005** `style.css` のベーススタイルとCSS変数を定義する

---

## フェーズ 1：データ層

- [x] **T-010** `storage.js` を実装する
  - getSubjects / getSessions / getSettings / getTimerState
  - saveSubject / deleteSubject
  - saveSession / deleteSession
  - saveSettings / saveTimerState
  - exportAll / importAll / clearAll / getStorageSize
- [x] **T-011** `storage.js` の動作を `console` で手動確認する

---

## フェーズ 2：ルーティング

- [x] **T-020** `router.js` を実装する（ハッシュ変化の監視・ページ切り替え）
- [x] **T-021** `app.js` を実装する（SW 登録・router 初期化・storage 初期化）
- [x] **T-022** ボトムナビゲーションの表示とアクティブ状態切り替えを実装する

---

## フェーズ 3：科目管理画面

- [x] **T-030** `pages/subjects.js` を実装する
  - 科目一覧の表示
  - 科目追加フォーム（名前・カラー選択）
  - 科目編集（インラインまたはモーダル）
  - 科目削除（確認ダイアログ）
- [x] **T-031** カラーピッカー（プリセット色 8 色）を実装する

---

## フェーズ 4：記録画面

- [x] **T-040** `timer.js` を実装する
  - 開始・一時停止・リセット
  - タイマー状態の LocalStorage 永続化
  - タブ再起動後の状態復元
- [ ] **T-041** `pages/record.js` を実装する
  - タイマー UI（表示・ボタン）
  - 科目選択ドロップダウン（subjects から動的生成）
  - 手動入力フォーム（日付・時間・メモ）
  - タイマー停止時のセッション保存フロー
  - 手動入力のバリデーションとエラー表示

---

## フェーズ 5：ダッシュボード

- [ ] **T-050** `charts.js` を実装する（Chart.js ラッパー）
  - 棒グラフ（週別学習時間）
  - 円グラフ（科目別割合）
  - インスタンス管理（画面離脱時に destroy）
- [ ] **T-051** `pages/dashboard.js` を実装する
  - 今日の合計学習時間
  - 今週の合計学習時間
  - 科目別グラフの描画
  - 科目が 0 件 / セッションが 0 件のエンプティステート表示

---

## フェーズ 6：履歴画面

- [ ] **T-060** `pages/history.js` を実装する
  - セッション一覧（日付降順）
  - 科目名・時間・メモの表示
  - 削除ボタン（確認ダイアログ）
  - 孤立セッション（削除済み科目）の表示対応

---

## フェーズ 7：設定画面

- [ ] **T-070** `pages/settings.js` を実装する
  - データエクスポート（JSON ダウンロード）
  - データインポート（ファイル選択・バリデーション・上書き確認）
  - データ全削除（二重確認ダイアログ）
  - ストレージ使用量の表示

---

## フェーズ 8：PWA 対応

- [ ] **T-080** `sw.js` を実装する（Cache First 戦略・precache リスト）
- [ ] **T-081** オフライン動作を DevTools で確認する
- [ ] **T-082** ホーム画面への追加（インストールプロンプト）を実装する
- [ ] **T-083** `manifest.json` の動作確認（Lighthouse で PWA スコア確認）

---

## フェーズ 9：スタイリング仕上げ

- [ ] **T-090** 全画面のモバイルレイアウトを仕上げる（375px 基準）
- [ ] **T-091** タブレット・PC のレイアウト対応（768px 以上）
- [ ] **T-092** ローディング・トースト通知の実装
- [ ] **T-093** エンプティステートのデザイン（科目未登録・記録なし）

---

## フェーズ 10：デプロイ

- [ ] **T-100** GitHub リポジトリを作成してプッシュする
- [ ] **T-101** GitHub Pages を有効化する（Settings > Pages）
- [ ] **T-102** 本番 URL で動作確認する（特に SW・オフライン）
- [ ] **T-103** Lighthouse でスコア確認（PWA・Performance・Accessibility）

---

## 完了基準（MVP リリース条件）

- [ ] 5画面すべてが動作する
- [ ] タイマーでセッションを記録できる
- [ ] 手動入力でセッションを記録できる
- [ ] ダッシュボードにグラフが表示される
- [ ] オフラインで全機能が動作する（DevTools > Network: Offline で確認）
- [ ] スマホ（375px）でレイアウトが崩れない
- [ ] ホーム画面に追加できる
- [ ] Lighthouse PWA スコア: 緑（基準クリア）
